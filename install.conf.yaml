- clean: ["~"]

- link:
    ~/.zshrc: .zshrc
    ~/.vim: .vim/
    ~/.vimrc: .vimrc
    ~/.gitconfig: .gitconfig
    ~/.tmux.conf: .tmux.conf

- shell: [git submodule update --init --recursive, Installing submodules]
