- clean: ["~"]

- link: 
    ~/.bashrc: .bashrc
    ~/.zshrc: .zshrc
    ~/.vim: .vim/
    ~/.vimrc: .vimrc
    ~/.gitconfig: .gitconfig
    ~/.Xmodmap: .Xmodmap

- shell: [git submodule update --init --recursive, Installing submodules]
